
<view>
	<block>
		<message
				title="{{waitingTitle}}"
				subTitle="{{waitingSubtitle}}"
				mainButton="{{waitingButton.mainButton}}"
				onTapMain="next"
				type="warn">
		</message>
	</block>
</view>
